package mantra

func setFlags() {
	// flagSet := flag.NewFlagSet("mantra", flag.ContinueOnError)

	// nodeAlias := flagSet.String("name", "", "Node's name")
	// fmt.Println(nodeAlias)

	// err := flagSet.Parse(os.Args[1:])
	// if err != nil {
	// 	log.Error(err)
	// }
}
